[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "failure_paths"
description = ""
readme = "README.md"
license = { text = "MIT" }

requires-python = ">=3.12"
dynamic = ["version"]

[tool.hatch.version]
path = "failure_paths/__init__.py"
